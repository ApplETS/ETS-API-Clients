name: ets_api_clients
description: API clients to help process calls from any application that need to access 
version: 0.4.1
homepage: https://clubapplets.ca/
repository: https://github.com/ApplETS/ETS-API-Clients
issue_tracker: https://github.com/ApplETS/ETS-API-Clients/issues
documentation: https://github.com/ApplETS/ETS-API-Clients

environment:
  sdk: ">=2.10.0 <3.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  
  xml: ^5.1.0
  http: ^0.13.0
  intl: ^0.17.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^1.0.0
  mockito: ^5.0.5
  
flutter: